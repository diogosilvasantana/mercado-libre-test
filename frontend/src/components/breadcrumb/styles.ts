import { styled } from "@stitches/react";

export const BreadcrumbContainer = styled("div", {
  paddingBlock: "1rem",

  span: {
    fontFamily: "arial",
    fontSize: "14px",
    color: "$grey500",
  },
});
